{
  "info": {
    "_postman_id": "0c664def-6159-4c53-b082-4b67afdf0d53",
    "name": "Check_selection_logic",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "_exporter_id": "50622847",
    "_collection_link": "https://go.postman.co/collection/50622847-0c664def-6159-4c53-b082-4b67afdf0d53?source=collection_link"
  },
  "item": [
    {
      "name": "Newest",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "/**",
              " * Postman Tests:",
              " * - Verify select_logic nodes are sorted by publish_date DESC (new -> old) when publish_date exists",
              " * - Verify filter_pin_exclude_live sorted by publish_date DESC",
              " * - Verify filter_live_exclude_pin sorted by CreateTime DESC",
              " * - Verify filter nodes structure is valid",
              " *",
              " * Supported nodes:",
              " * - select_logic_sfv_google_trend",
              " * - select_logic_sfv_relevance_creator",
              " * - select_logic_sfv_viral",
              " * - select_logic_sfv_history",
              " * - filter_pin_exclude_live",
              " * - filter_live_exclude_pin",
              " */",
              "",
              "// =============================",
              "// Helpers",
              "// =============================",
              "function deepFindNode(obj, key) {",
              "  if (obj && typeof obj === \"object\") {",
              "    if (",
              "      Object.prototype.hasOwnProperty.call(obj, key) &&",
              "      obj[key] &&",
              "      typeof obj[key] === \"object\"",
              "    ) {",
              "      return obj[key];",
              "    }",
              "",
              "    if (Array.isArray(obj)) {",
              "      for (const v of obj) {",
              "        const found = deepFindNode(v, key);",
              "        if (found) return found;",
              "      }",
              "    } else {",
              "      for (const k of Object.keys(obj)) {",
              "        const found = deepFindNode(obj[k], key);",
              "        if (found) return found;",
              "      }",
              "    }",
              "  }",
              "  return null;",
              "}",
              "",
              "function toMillis(iso) {",
              "  const ms = Date.parse(iso);",
              "  return Number.isFinite(ms) ? ms : NaN;",
              "}",
              "",
              "function toEpochSeconds(val) {",
              "  const num = Number(val);",
              "  return Number.isFinite(num) ? num : NaN;",
              "}",
              "",
              "function pickOrderIds(result) {",
              "  if (result && Array.isArray(result.ids) && result.ids.length > 0) {",
              "    return result.ids;",
              "  }",
              "",
              "  if (result && Array.isArray(result.seenItems) && result.seenItems.length > 0) {",
              "    return result.seenItems;",
              "  }",
              "",
              "  if (result && Array.isArray(result.items) && result.items.length > 0) {",
              "    return result.items",
              "      .map(it => {",
              "        if (!it || typeof it !== \"object\") return null;",
              "        return it.id ?? it.Id ?? it.ActivityId ?? null;",
              "      })",
              "      .filter(v => v !== null && v !== undefined);",
              "  }",
              "",
              "  return [];",
              "}",
              "",
              "function buildIdToPublishDate(result) {",
              "  const map = {};",
              "",
              "  const items = []",
              "    .concat(Array.isArray(result && result.items) ? result.items : [])",
              "    .concat(Array.isArray(result && result.targetItems) ? result.targetItems : []);",
              "",
              "  for (const it of items) {",
              "    if (!it || typeof it !== \"object\") continue;",
              "",
              "    const id = it.id ?? it.Id ?? it.ActivityId;",
              "    if (id !== null && id !== undefined && typeof it.publish_date === \"string\") {",
              "      map[String(id)] = it.publish_date;",
              "    }",
              "  }",
              "",
              "  return map;",
              "}",
              "",
              "function buildIdToCreateTime(result) {",
              "  const map = {};",
              "",
              "  const items = []",
              "    .concat(Array.isArray(result && result.items) ? result.items : [])",
              "    .concat(Array.isArray(result && result.targetItems) ? result.targetItems : []);",
              "",
              "  for (const it of items) {",
              "    if (!it || typeof it !== \"object\") continue;",
              "",
              "    const id = it.id ?? it.Id ?? it.ActivityId;",
              "    if (id !== null && id !== undefined && it.CreateTime !== undefined && it.CreateTime !== null) {",
              "      map[String(id)] = it.CreateTime;",
              "    }",
              "  }",
              "",
              "  return map;",
              "}",
              "",
              "// =============================",
              "// Validators",
              "// =============================",
              "function verifyNodeExists(nodeName, body) {",
              "  pm.test(`[${nodeName}] node exists`, function () {",
              "    const node = deepFindNode(body, nodeName);",
              "    pm.expect(node, `Cannot find node '${nodeName}' in response`).to.not.equal(null);",
              "  });",
              "",
              "  return deepFindNode(body, nodeName);",
              "}",
              "",
              "function verifySelectLogicSortedByPublishDateDesc(nodeName, body) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "",
              "  pm.test(`[${nodeName}] has result`, function () {",
              "    pm.expect(result, \"node.result missing\").to.be.an(\"object\");",
              "  });",
              "",
              "  const orderIds = pickOrderIds(result).map(String);",
              "",
              "  pm.test(`[${nodeName}] has order ids`, function () {",
              "    pm.expect(",
              "      orderIds.length,",
              "      \"No ids found in result.ids/result.seenItems/result.items\"",
              "    ).to.be.above(0);",
              "  });",
              "",
              "  const id2date = buildIdToPublishDate(result);",
              "  const idsWithDate = orderIds.filter(id => !!id2date[id]);",
              "  const missing = orderIds.filter(id => !id2date[id]);",
              "",
              "  if (idsWithDate.length === 0) {",
              "    pm.test(`[${nodeName}] skip publish_date DESC check (no publish_date payload)`, function () {",
              "      pm.expect(true).to.equal(true);",
              "    });",
              "",
              "    console.warn(`\\n[${nodeName}] SKIPPED publish_date DESC check`);",
              "    console.warn(`[${nodeName}] reason: result has ids but no items/targetItems with publish_date`);",
              "    console.warn(`[${nodeName}] ids: ${orderIds.join(\", \")}`);",
              "    return;",
              "  }",
              "",
              "  pm.test(`[${nodeName}] all order ids have publish_date`, function () {",
              "    pm.expect(missing, `Missing publish_date for ids: ${missing.join(\", \")}`).to.have.lengthOf(0);",
              "  });",
              "",
              "  pm.test(`[${nodeName}] publish_date is sorted DESC (new -> old)`, function () {",
              "    const problems = [];",
              "",
              "    for (let i = 0; i < orderIds.length - 1; i++) {",
              "      const aId = orderIds[i];",
              "      const bId = orderIds[i + 1];",
              "      const aIso = id2date[aId];",
              "      const bIso = id2date[bId];",
              "",
              "      const aMs = toMillis(aIso);",
              "      const bMs = toMillis(bIso);",
              "",
              "      if (!Number.isFinite(aMs) || !Number.isFinite(bMs)) {",
              "        problems.push(`Invalid date parse: ${aId}=${aIso}, ${bId}=${bIso}`);",
              "        continue;",
              "      }",
              "",
              "      if (aMs < bMs) {",
              "        problems.push(`Violation at ${i}->${i + 1}: ${aId}(${aIso}) < ${bId}(${bIso})`);",
              "      }",
              "    }",
              "",
              "    pm.expect(problems, problems.join(\" | \")).to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] order check (id -> publish_date):`);",
              "  orderIds.forEach((id, idx) => {",
              "    console.log(`${String(idx).padStart(2, \"0\")}  ${id}  ${id2date[id] || \"MISSING\"}`);",
              "  });",
              "}",
              "",
              "function verifyFilterNode(nodeName, body, allowEmptyItems = true) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "",
              "  pm.test(`[${nodeName}] has result`, function () {",
              "    pm.expect(result, \"node.result missing\").to.be.an(\"object\");",
              "  });",
              "",
              "  pm.test(`[${nodeName}] has result.items array`, function () {",
              "    pm.expect(Array.isArray(result && result.items), \"result.items missing\").to.equal(true);",
              "  });",
              "",
              "  const items = Array.isArray(result && result.items) ? result.items : [];",
              "",
              "  pm.test(`[${nodeName}] item_size matches items.length`, function () {",
              "    pm.expect(result.item_size, \"result.item_size missing\").to.equal(items.length);",
              "  });",
              "",
              "  if (!allowEmptyItems) {",
              "    pm.test(`[${nodeName}] items should not be empty`, function () {",
              "      pm.expect(items.length, \"result.items is empty\").to.be.above(0);",
              "    });",
              "  }",
              "",
              "  pm.test(`[${nodeName}] items are valid objects`, function () {",
              "    const invalid = items.filter(it => !it || typeof it !== \"object\");",
              "    pm.expect(invalid, \"Some items are not valid objects\").to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] items:`);",
              "  if (items.length === 0) {",
              "    console.log(\"(empty)\");",
              "    return;",
              "  }",
              "",
              "  items.forEach((it, idx) => {",
              "    const displayValue =",
              "      it && typeof it === \"object\"",
              "        ? (it.id ?? it.Id ?? it.ActivityId ?? JSON.stringify(it))",
              "        : String(it);",
              "",
              "    console.log(`${String(idx).padStart(2, \"0\")}  ${displayValue}`);",
              "  });",
              "}",
              "",
              "function verifyFilterPinExcludeLiveSortedByPublishDateDesc(nodeName, body) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "  const items = Array.isArray(result && result.items) ? result.items : [];",
              "",
              "  pm.test(`[${nodeName}] all items have publish_date`, function () {",
              "    const missing = items.filter(it => !it || typeof it.publish_date !== \"string\");",
              "    pm.expect(missing, \"Some items missing publish_date\").to.have.lengthOf(0);",
              "  });",
              "",
              "  pm.test(`[${nodeName}] publish_date is sorted DESC (new -> old)`, function () {",
              "    const problems = [];",
              "",
              "    for (let i = 0; i < items.length - 1; i++) {",
              "      const a = items[i];",
              "      const b = items[i + 1];",
              "",
              "      const aMs = toMillis(a.publish_date);",
              "      const bMs = toMillis(b.publish_date);",
              "",
              "      if (!Number.isFinite(aMs) || !Number.isFinite(bMs)) {",
              "        problems.push(",
              "          `Invalid date parse: ${a.id}=${a.publish_date}, ${b.id}=${b.publish_date}`",
              "        );",
              "        continue;",
              "      }",
              "",
              "      if (aMs < bMs) {",
              "        problems.push(",
              "          `Violation at ${i}->${i + 1}: ${a.id}(${a.publish_date}) < ${b.id}(${b.publish_date})`",
              "        );",
              "      }",
              "    }",
              "",
              "    pm.expect(problems, problems.join(\" | \")).to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] publish_date order check:`);",
              "  items.forEach((it, idx) => {",
              "    console.log(`${String(idx).padStart(2, \"0\")}  ${it.id}  ${it.publish_date}`);",
              "  });",
              "}",
              "",
              "function verifyFilterLiveExcludePinSortedByCreateTimeDesc(nodeName, body) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "  const items = Array.isArray(result && result.items) ? result.items : [];",
              "",
              "  pm.test(`[${nodeName}] all items have CreateTime`, function () {",
              "    const missing = items.filter(it => !it || !Number.isFinite(Number(it.CreateTime)));",
              "    pm.expect(missing, \"Some items missing/invalid CreateTime\").to.have.lengthOf(0);",
              "  });",
              "",
              "  pm.test(`[${nodeName}] CreateTime is sorted DESC (new -> old)`, function () {",
              "    const problems = [];",
              "",
              "    for (let i = 0; i < items.length - 1; i++) {",
              "      const a = items[i];",
              "      const b = items[i + 1];",
              "",
              "      const aTs = toEpochSeconds(a.CreateTime);",
              "      const bTs = toEpochSeconds(b.CreateTime);",
              "",
              "      if (!Number.isFinite(aTs) || !Number.isFinite(bTs)) {",
              "        problems.push(",
              "          `Invalid CreateTime: ${a.ActivityId || a.Id}=${a.CreateTime}, ${b.ActivityId || b.Id}=${b.CreateTime}`",
              "        );",
              "        continue;",
              "      }",
              "",
              "      if (aTs < bTs) {",
              "        problems.push(",
              "          `Violation at ${i}->${i + 1}: ${a.ActivityId || a.Id}(${a.CreateTime}) < ${b.ActivityId || b.Id}(${b.CreateTime})`",
              "        );",
              "      }",
              "    }",
              "",
              "    pm.expect(problems, problems.join(\" | \")).to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] CreateTime order check:`);",
              "  items.forEach((it, idx) => {",
              "    console.log(",
              "      `${String(idx).padStart(2, \"0\")}  ${it.ActivityId || it.Id}  ${it.CreateTime}`",
              "    );",
              "  });",
              "}",
              "",
              "// =============================",
              "// Entry",
              "// =============================",
              "pm.test(\"HTTP 200\", function () {",
              "  pm.response.to.have.status(200);",
              "});",
              "",
              "let body;",
              "pm.test(\"Response is valid JSON\", function () {",
              "  body = pm.response.json();",
              "  pm.expect(body).to.be.an(\"object\");",
              "});",
              "",
              "// =============================",
              "// Run",
              "// =============================",
              "const SELECT_LOGIC_PUBLISH_DATE_NODES = [",
              "  \"select_logic_sfv_google_trend\",",
              "  \"select_logic_sfv_relevance_creator\",",
              "  \"select_logic_sfv_viral\",",
              "  \"select_logic_sfv_history\"",
              "];",
              "",
              "SELECT_LOGIC_PUBLISH_DATE_NODES.forEach(nodeName => {",
              "  verifySelectLogicSortedByPublishDateDesc(nodeName, body);",
              "});",
              "",
              "verifyFilterNode(\"filter_pin_exclude_live\", body, false);",
              "verifyFilterNode(\"filter_live_exclude_pin\", body, true);",
              "",
              "verifyFilterPinExcludeLiveSortedByPublishDateDesc(\"filter_pin_exclude_live\", body);",
              "verifyFilterLiveExcludePinSortedByCreateTimeDesc(\"filter_live_exclude_pin\", body);"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "http://poc-ai-universal-platform.preprod-gcp-ai-bn.int-ai-platform.gcp.dmp.true.th/api/v1/sfv-p4?ssoId=9&verbose=debug&shelfid=BJq5rZqYzjgJ&node_google_select_logic=by_newest&node_pin_select_logic=by_newest&node_live_select_logic=by_newest&node_viral_select_logic=by_newest&node_creator_select_logic=by_newest&node_history_select_logic=by_newest",
          "protocol": "http",
          "host": [
            "poc-ai-universal-platform",
            "preprod-gcp-ai-bn",
            "int-ai-platform",
            "gcp",
            "dmp",
            "true",
            "th"
          ],
          "path": [
            "api",
            "v1",
            "sfv-p4"
          ],
          "query": [
            {
              "key": "ssoId",
              "value": "9"
            },
            {
              "key": "verbose",
              "value": "debug"
            },
            {
              "key": "node_history_block_size",
              "value": "6",
              "disabled": true
            },
            {
              "key": "node_history_calculate_type",
              "value": "fix",
              "disabled": true
            },
            {
              "key": "node_history_items_per_block",
              "value": "1",
              "disabled": true
            },
            {
              "key": "node_history_number_first",
              "value": "0",
              "disabled": true
            },
            {
              "key": "node_history_positions",
              "value": "0",
              "disabled": true
            },
            {
              "key": "node_pin_block_size",
              "value": "6",
              "disabled": true
            },
            {
              "key": "node_pin_calculate_type",
              "value": "fix",
              "disabled": true
            },
            {
              "key": "node_pin_items_per_block",
              "value": "1",
              "disabled": true
            },
            {
              "key": "node_pin_number_first",
              "value": "0",
              "disabled": true
            },
            {
              "key": "node_pin_positions",
              "value": "1",
              "disabled": true
            },
            {
              "key": "node_live_block_size",
              "value": "6",
              "disabled": true
            },
            {
              "key": "node_live_calculate_type",
              "value": "fix",
              "disabled": true
            },
            {
              "key": "node_live_items_per_block",
              "value": "1",
              "disabled": true
            },
            {
              "key": "node_live_number_first",
              "value": "0",
              "disabled": true
            },
            {
              "key": "node_live_positions",
              "value": "1,2",
              "disabled": true
            },
            {
              "key": "node_google_block_size",
              "value": "6",
              "disabled": true
            },
            {
              "key": "node_google_calculate_type",
              "value": "fix",
              "disabled": true
            },
            {
              "key": "node_google_items_per_block",
              "value": "1",
              "disabled": true
            },
            {
              "key": "node_google_number_first",
              "value": "0",
              "disabled": true
            },
            {
              "key": "node_google_positions",
              "value": "4",
              "disabled": true
            },
            {
              "key": "node_viral_block_size",
              "value": "6",
              "disabled": true
            },
            {
              "key": "node_viral_calculate_type",
              "value": "fix",
              "disabled": true
            },
            {
              "key": "node_viral_items_per_block",
              "value": "1",
              "disabled": true
            },
            {
              "key": "node_viral_number_first",
              "value": "0",
              "disabled": true
            },
            {
              "key": "node_viral_positions",
              "value": "5",
              "disabled": true
            },
            {
              "key": "node_creator_block_size",
              "value": "6",
              "disabled": true
            },
            {
              "key": "node_creator_calculate_type",
              "value": "random",
              "disabled": true
            },
            {
              "key": "node_creator_items_per_block",
              "value": "1",
              "disabled": true
            },
            {
              "key": "node_creator_number_first",
              "value": "0",
              "disabled": true
            },
            {
              "key": "node_creator_positions",
              "value": "3",
              "disabled": true
            },
            {
              "key": "shelfid",
              "value": "BJq5rZqYzjgJ"
            },
            {
              "key": "node_google_select_logic",
              "value": "by_newest"
            },
            {
              "key": "node_pin_select_logic",
              "value": "by_newest"
            },
            {
              "key": "node_live_select_logic",
              "value": "by_newest"
            },
            {
              "key": "node_viral_select_logic",
              "value": "by_newest"
            },
            {
              "key": "node_creator_select_logic",
              "value": "by_newest"
            },
            {
              "key": "node_history_select_logic",
              "value": "by_newest"
            },
            {
              "key": "limit_seen_items",
              "value": "55",
              "type": "text",
              "disabled": true
            },
            {
              "key": "cmsIds",
              "value": "zVRY3EZBmxa4",
              "type": "text",
              "disabled": true
            }
          ]
        }
      },
      "response": []
    },
    {
      "name": "Oldest",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "/**",
              " * Postman Tests:",
              " * - Verify select_logic nodes are sorted by publish_date ASC (old -> new) when publish_date exists",
              " * - Verify filter_pin_exclude_live sorted by publish_date ASC",
              " * - Verify filter_live_exclude_pin sorted by CreateTime ASC",
              " * - Verify filter nodes structure is valid",
              " *",
              " * Supported nodes:",
              " * - select_logic_sfv_google_trend",
              " * - select_logic_sfv_relevance_creator",
              " * - select_logic_sfv_viral",
              " * - select_logic_sfv_history",
              " * - filter_pin_exclude_live",
              " * - filter_live_exclude_pin",
              " */",
              "",
              "// =============================",
              "// Helpers",
              "// =============================",
              "function deepFindNode(obj, key) {",
              "  if (obj && typeof obj === \"object\") {",
              "    if (",
              "      Object.prototype.hasOwnProperty.call(obj, key) &&",
              "      obj[key] &&",
              "      typeof obj[key] === \"object\"",
              "    ) {",
              "      return obj[key];",
              "    }",
              "",
              "    if (Array.isArray(obj)) {",
              "      for (const v of obj) {",
              "        const found = deepFindNode(v, key);",
              "        if (found) return found;",
              "      }",
              "    } else {",
              "      for (const k of Object.keys(obj)) {",
              "        const found = deepFindNode(obj[k], key);",
              "        if (found) return found;",
              "      }",
              "    }",
              "  }",
              "  return null;",
              "}",
              "",
              "function toMillis(iso) {",
              "  const ms = Date.parse(iso);",
              "  return Number.isFinite(ms) ? ms : NaN;",
              "}",
              "",
              "function toEpochSeconds(val) {",
              "  const num = Number(val);",
              "  return Number.isFinite(num) ? num : NaN;",
              "}",
              "",
              "function pickOrderIds(result) {",
              "  if (result && Array.isArray(result.ids) && result.ids.length > 0) {",
              "    return result.ids;",
              "  }",
              "",
              "  if (result && Array.isArray(result.seenItems) && result.seenItems.length > 0) {",
              "    return result.seenItems;",
              "  }",
              "",
              "  if (result && Array.isArray(result.items) && result.items.length > 0) {",
              "    return result.items",
              "      .map(it => {",
              "        if (!it || typeof it !== \"object\") return null;",
              "        return it.id ?? it.Id ?? it.ActivityId ?? null;",
              "      })",
              "      .filter(v => v !== null && v !== undefined);",
              "  }",
              "",
              "  return [];",
              "}",
              "",
              "function buildIdToPublishDate(result) {",
              "  const map = {};",
              "",
              "  const items = []",
              "    .concat(Array.isArray(result && result.items) ? result.items : [])",
              "    .concat(Array.isArray(result && result.targetItems) ? result.targetItems : []);",
              "",
              "  for (const it of items) {",
              "    if (!it || typeof it !== \"object\") continue;",
              "",
              "    const id = it.id ?? it.Id ?? it.ActivityId;",
              "    if (id !== null && id !== undefined && typeof it.publish_date === \"string\") {",
              "      map[String(id)] = it.publish_date;",
              "    }",
              "  }",
              "",
              "  return map;",
              "}",
              "",
              "function buildIdToCreateTime(result) {",
              "  const map = {};",
              "",
              "  const items = []",
              "    .concat(Array.isArray(result && result.items) ? result.items : [])",
              "    .concat(Array.isArray(result && result.targetItems) ? result.targetItems : []);",
              "",
              "  for (const it of items) {",
              "    if (!it || typeof it !== \"object\") continue;",
              "",
              "    const id = it.id ?? it.Id ?? it.ActivityId;",
              "    if (id !== null && id !== undefined && it.CreateTime !== undefined && it.CreateTime !== null) {",
              "      map[String(id)] = it.CreateTime;",
              "    }",
              "  }",
              "",
              "  return map;",
              "}",
              "",
              "// =============================",
              "// Validators",
              "// =============================",
              "function verifyNodeExists(nodeName, body) {",
              "  pm.test(`[${nodeName}] node exists`, function () {",
              "    const node = deepFindNode(body, nodeName);",
              "    pm.expect(node, `Cannot find node '${nodeName}' in response`).to.not.equal(null);",
              "  });",
              "",
              "  return deepFindNode(body, nodeName);",
              "}",
              "",
              "function verifySelectLogicSortedByPublishDateAsc(nodeName, body) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "",
              "  pm.test(`[${nodeName}] has result`, function () {",
              "    pm.expect(result, \"node.result missing\").to.be.an(\"object\");",
              "  });",
              "",
              "  const orderIds = pickOrderIds(result).map(String);",
              "",
              "  pm.test(`[${nodeName}] has order ids`, function () {",
              "    pm.expect(",
              "      orderIds.length,",
              "      \"No ids found in result.ids/result.seenItems/result.items\"",
              "    ).to.be.above(0);",
              "  });",
              "",
              "  const id2date = buildIdToPublishDate(result);",
              "  const idsWithDate = orderIds.filter(id => !!id2date[id]);",
              "  const missing = orderIds.filter(id => !id2date[id]);",
              "",
              "  if (idsWithDate.length === 0) {",
              "    pm.test(`[${nodeName}] skip publish_date ASC check (no publish_date payload)`, function () {",
              "      pm.expect(true).to.equal(true);",
              "    });",
              "",
              "    console.warn(`\\n[${nodeName}] SKIPPED publish_date ASC check`);",
              "    console.warn(`[${nodeName}] reason: result has ids but no items/targetItems with publish_date`);",
              "    console.warn(`[${nodeName}] ids: ${orderIds.join(\", \")}`);",
              "    return;",
              "  }",
              "",
              "  pm.test(`[${nodeName}] all order ids have publish_date`, function () {",
              "    pm.expect(missing, `Missing publish_date for ids: ${missing.join(\", \")}`).to.have.lengthOf(0);",
              "  });",
              "",
              "  pm.test(`[${nodeName}] publish_date is sorted ASC (old -> new)`, function () {",
              "    const problems = [];",
              "",
              "    for (let i = 0; i < orderIds.length - 1; i++) {",
              "      const aId = orderIds[i];",
              "      const bId = orderIds[i + 1];",
              "      const aIso = id2date[aId];",
              "      const bIso = id2date[bId];",
              "",
              "      const aMs = toMillis(aIso);",
              "      const bMs = toMillis(bIso);",
              "",
              "      if (!Number.isFinite(aMs) || !Number.isFinite(bMs)) {",
              "        problems.push(`Invalid date parse: ${aId}=${aIso}, ${bId}=${bIso}`);",
              "        continue;",
              "      }",
              "",
              "      if (aMs > bMs) {",
              "        problems.push(`Violation at ${i}->${i + 1}: ${aId}(${aIso}) > ${bId}(${bIso})`);",
              "      }",
              "    }",
              "",
              "    pm.expect(problems, problems.join(\" | \")).to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] order check (id -> publish_date):`);",
              "  orderIds.forEach((id, idx) => {",
              "    console.log(`${String(idx).padStart(2, \"0\")}  ${id}  ${id2date[id] || \"MISSING\"}`);",
              "  });",
              "}",
              "",
              "function verifyFilterNode(nodeName, body, allowEmptyItems = true) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "",
              "  pm.test(`[${nodeName}] has result`, function () {",
              "    pm.expect(result, \"node.result missing\").to.be.an(\"object\");",
              "  });",
              "",
              "  pm.test(`[${nodeName}] has result.items array`, function () {",
              "    pm.expect(Array.isArray(result && result.items), \"result.items missing\").to.equal(true);",
              "  });",
              "",
              "  const items = Array.isArray(result && result.items) ? result.items : [];",
              "",
              "  pm.test(`[${nodeName}] item_size matches items.length`, function () {",
              "    pm.expect(result.item_size, \"result.item_size missing\").to.equal(items.length);",
              "  });",
              "",
              "  if (!allowEmptyItems) {",
              "    pm.test(`[${nodeName}] items should not be empty`, function () {",
              "      pm.expect(items.length, \"result.items is empty\").to.be.above(0);",
              "    });",
              "  }",
              "",
              "  pm.test(`[${nodeName}] items are valid objects`, function () {",
              "    const invalid = items.filter(it => !it || typeof it !== \"object\");",
              "    pm.expect(invalid, \"Some items are not valid objects\").to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] items:`);",
              "  if (items.length === 0) {",
              "    console.log(\"(empty)\");",
              "    return;",
              "  }",
              "",
              "  items.forEach((it, idx) => {",
              "    const displayValue =",
              "      it && typeof it === \"object\"",
              "        ? (it.id ?? it.Id ?? it.ActivityId ?? JSON.stringify(it))",
              "        : String(it);",
              "",
              "    console.log(`${String(idx).padStart(2, \"0\")}  ${displayValue}`);",
              "  });",
              "}",
              "",
              "function verifyFilterPinExcludeLiveSortedByPublishDateAsc(nodeName, body) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "  const items = Array.isArray(result && result.items) ? result.items : [];",
              "",
              "  pm.test(`[${nodeName}] all items have publish_date`, function () {",
              "    const missing = items.filter(it => !it || typeof it.publish_date !== \"string\");",
              "    pm.expect(missing, \"Some items missing publish_date\").to.have.lengthOf(0);",
              "  });",
              "",
              "  pm.test(`[${nodeName}] publish_date is sorted ASC (old -> new)`, function () {",
              "    const problems = [];",
              "",
              "    for (let i = 0; i < items.length - 1; i++) {",
              "      const a = items[i];",
              "      const b = items[i + 1];",
              "",
              "      const aMs = toMillis(a.publish_date);",
              "      const bMs = toMillis(b.publish_date);",
              "",
              "      if (!Number.isFinite(aMs) || !Number.isFinite(bMs)) {",
              "        problems.push(",
              "          `Invalid date parse: ${a.id}=${a.publish_date}, ${b.id}=${b.publish_date}`",
              "        );",
              "        continue;",
              "      }",
              "",
              "      if (aMs > bMs) {",
              "        problems.push(",
              "          `Violation at ${i}->${i + 1}: ${a.id}(${a.publish_date}) > ${b.id}(${b.publish_date})`",
              "        );",
              "      }",
              "    }",
              "",
              "    pm.expect(problems, problems.join(\" | \")).to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] publish_date order check:`);",
              "  items.forEach((it, idx) => {",
              "    console.log(`${String(idx).padStart(2, \"0\")}  ${it.id}  ${it.publish_date}`);",
              "  });",
              "}",
              "",
              "function verifyFilterLiveExcludePinSortedByCreateTimeAsc(nodeName, body) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "  const items = Array.isArray(result && result.items) ? result.items : [];",
              "",
              "  pm.test(`[${nodeName}] all items have CreateTime`, function () {",
              "    const missing = items.filter(it => !it || !Number.isFinite(Number(it.CreateTime)));",
              "    pm.expect(missing, \"Some items missing/invalid CreateTime\").to.have.lengthOf(0);",
              "  });",
              "",
              "  pm.test(`[${nodeName}] CreateTime is sorted ASC (old -> new)`, function () {",
              "    const problems = [];",
              "",
              "    for (let i = 0; i < items.length - 1; i++) {",
              "      const a = items[i];",
              "      const b = items[i + 1];",
              "",
              "      const aTs = toEpochSeconds(a.CreateTime);",
              "      const bTs = toEpochSeconds(b.CreateTime);",
              "",
              "      if (!Number.isFinite(aTs) || !Number.isFinite(bTs)) {",
              "        problems.push(",
              "          `Invalid CreateTime: ${a.ActivityId || a.Id}=${a.CreateTime}, ${b.ActivityId || b.Id}=${b.CreateTime}`",
              "        );",
              "        continue;",
              "      }",
              "",
              "      if (aTs > bTs) {",
              "        problems.push(",
              "          `Violation at ${i}->${i + 1}: ${a.ActivityId || a.Id}(${a.CreateTime}) > ${b.ActivityId || b.Id}(${b.CreateTime})`",
              "        );",
              "      }",
              "    }",
              "",
              "    pm.expect(problems, problems.join(\" | \")).to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] CreateTime order check:`);",
              "  items.forEach((it, idx) => {",
              "    console.log(",
              "      `${String(idx).padStart(2, \"0\")}  ${it.ActivityId || it.Id}  ${it.CreateTime}`",
              "    );",
              "  });",
              "}",
              "",
              "// =============================",
              "// Entry",
              "// =============================",
              "pm.test(\"HTTP 200\", function () {",
              "  pm.response.to.have.status(200);",
              "});",
              "",
              "let body;",
              "pm.test(\"Response is valid JSON\", function () {",
              "  body = pm.response.json();",
              "  pm.expect(body).to.be.an(\"object\");",
              "});",
              "",
              "// =============================",
              "// Run",
              "// =============================",
              "const SELECT_LOGIC_PUBLISH_DATE_NODES = [",
              "  \"select_logic_sfv_google_trend\",",
              "  \"select_logic_sfv_relevance_creator\",",
              "  \"select_logic_sfv_viral\",",
              "  \"select_logic_sfv_history\"",
              "];",
              "",
              "SELECT_LOGIC_PUBLISH_DATE_NODES.forEach(nodeName => {",
              "  verifySelectLogicSortedByPublishDateAsc(nodeName, body);",
              "});",
              "",
              "verifyFilterNode(\"filter_pin_exclude_live\", body, false);",
              "verifyFilterNode(\"filter_live_exclude_pin\", body, true);",
              "",
              "verifyFilterPinExcludeLiveSortedByPublishDateAsc(\"filter_pin_exclude_live\", body);",
              "verifyFilterLiveExcludePinSortedByCreateTimeAsc(\"filter_live_exclude_pin\", body);"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "http://poc-ai-universal-platform.preprod-gcp-ai-bn.int-ai-platform.gcp.dmp.true.th/api/v1/sfv-p4?ssoId=9&verbose=debug&node_google_select_logic=by_oldest&node_pin_select_logic=by_oldest&node_live_select_logic=by_oldest&node_viral_select_logic=by_oldest&node_creator_select_logic=by_oldest&node_history_select_logic=by_oldest&shelfid=BJq5rZqYzjgJ",
          "protocol": "http",
          "host": [
            "poc-ai-universal-platform",
            "preprod-gcp-ai-bn",
            "int-ai-platform",
            "gcp",
            "dmp",
            "true",
            "th"
          ],
          "path": [
            "api",
            "v1",
            "sfv-p4"
          ],
          "query": [
            {
              "key": "ssoId",
              "value": "9"
            },
            {
              "key": "verbose",
              "value": "debug"
            },
            {
              "key": "node_google_select_logic",
              "value": "by_oldest"
            },
            {
              "key": "node_pin_select_logic",
              "value": "by_oldest"
            },
            {
              "key": "node_live_select_logic",
              "value": "by_oldest"
            },
            {
              "key": "node_viral_select_logic",
              "value": "by_oldest"
            },
            {
              "key": "node_creator_select_logic",
              "value": "by_oldest"
            },
            {
              "key": "node_history_select_logic",
              "value": "by_oldest"
            },
            {
              "key": "cmsIds",
              "value": "zVRY3EZBmxa4",
              "disabled": true
            },
            {
              "key": "shelfid",
              "value": "BJq5rZqYzjgJ"
            }
          ]
        }
      },
      "response": []
    },
    {
      "name": "Publisher",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "/**",
              " * Postman Tests:",
              " * - Verify select_logic nodes are subsets of their source nodes (get_* / get_redis_*)",
              " * - Verify select_logic nodes preserve the same top-to-bottom order as their source nodes",
              " * - Verify filter nodes structure is valid",
              " * - Verify filter outputs are consistent with their source nodes",
              " *",
              " * Supported mappings:",
              " * - select_logic_sfv_google_trend       <- get_google_trend",
              " * - select_logic_sfv_relevance_creator  <- get_redis_relevance_creator",
              " * - select_logic_sfv_viral              <- get_redis_viral",
              " * - select_logic_sfv_history            <- get_redis_history",
              " * - filter_pin_exclude_live             <- get_candidate_pin_global",
              " * - filter_live_exclude_pin             <- get_all_live (by ActivityId)",
              " */",
              "",
              "// =============================",
              "// Helpers",
              "// =============================",
              "function deepFindNode(obj, key) {",
              "  if (obj && typeof obj === \"object\") {",
              "    if (",
              "      Object.prototype.hasOwnProperty.call(obj, key) &&",
              "      obj[key] &&",
              "      typeof obj[key] === \"object\"",
              "    ) {",
              "      return obj[key];",
              "    }",
              "",
              "    if (Array.isArray(obj)) {",
              "      for (const v of obj) {",
              "        const found = deepFindNode(v, key);",
              "        if (found) return found;",
              "      }",
              "    } else {",
              "      for (const k of Object.keys(obj)) {",
              "        const found = deepFindNode(obj[k], key);",
              "        if (found) return found;",
              "      }",
              "    }",
              "  }",
              "  return null;",
              "}",
              "",
              "function unique(arr) {",
              "  return [...new Set(arr)];",
              "}",
              "",
              "function pickOrderIds(result) {",
              "  if (result && Array.isArray(result.ids) && result.ids.length > 0) {",
              "    return result.ids.map(String);",
              "  }",
              "",
              "  if (result && Array.isArray(result.seenItems) && result.seenItems.length > 0) {",
              "    return result.seenItems.map(String);",
              "  }",
              "",
              "  if (result && Array.isArray(result.items) && result.items.length > 0) {",
              "    return result.items",
              "      .map(it => {",
              "        if (!it || typeof it !== \"object\") return null;",
              "        return it.id ?? it.Id ?? it.ActivityId ?? null;",
              "      })",
              "      .filter(v => v !== null && v !== undefined)",
              "      .map(String);",
              "  }",
              "",
              "  return [];",
              "}",
              "",
              "function pickIds(result) {",
              "  if (result && Array.isArray(result.ids) && result.ids.length > 0) {",
              "    return result.ids.map(String);",
              "  }",
              "",
              "  if (result && Array.isArray(result.items)) {",
              "    return result.items",
              "      .map(it => {",
              "        if (!it || typeof it !== \"object\") return null;",
              "        return it.id ?? it.Id ?? it.ActivityId ?? null;",
              "      })",
              "      .filter(v => v !== null && v !== undefined)",
              "      .map(String);",
              "  }",
              "",
              "  return [];",
              "}",
              "",
              "function pickActivityIds(result) {",
              "  if (!result || !Array.isArray(result.items)) return [];",
              "  return result.items",
              "    .map(it => it && (it.ActivityId ?? it.Id ?? it.activity_id ?? it.id))",
              "    .filter(v => v !== null && v !== undefined)",
              "    .map(String);",
              "}",
              "",
              "// =============================",
              "// Validators",
              "// =============================",
              "function verifyNodeExists(nodeName, body) {",
              "  pm.test(`[${nodeName}] node exists`, function () {",
              "    const node = deepFindNode(body, nodeName);",
              "    pm.expect(node, `Cannot find node '${nodeName}' in response`).to.not.equal(null);",
              "  });",
              "",
              "  return deepFindNode(body, nodeName);",
              "}",
              "",
              "function verifySelectLogicSubsetOfSource(selectLogicNodeName, sourceNodeName, body) {",
              "  const selectLogicNode = verifyNodeExists(selectLogicNodeName, body);",
              "  const sourceNode = verifyNodeExists(sourceNodeName, body);",
              "  if (!selectLogicNode || !sourceNode) return;",
              "",
              "  const selectLogicIds = unique(pickIds(selectLogicNode.result));",
              "  const sourceIds = unique(pickIds(sourceNode.result));",
              "",
              "  pm.test(`[${selectLogicNodeName}] has ids`, function () {",
              "    pm.expect(selectLogicIds.length, \"select_logic ids missing\").to.be.above(0);",
              "  });",
              "",
              "  pm.test(`[${sourceNodeName}] has ids`, function () {",
              "    pm.expect(sourceIds.length, \"Source ids missing\").to.be.above(0);",
              "  });",
              "",
              "  pm.test(`[${selectLogicNodeName}] all ids must exist in [${sourceNodeName}]`, function () {",
              "    const sourceSet = new Set(sourceIds);",
              "    const missing = selectLogicIds.filter(id => !sourceSet.has(id));",
              "    pm.expect(missing, `Ids not found in ${sourceNodeName}: ${missing.join(\", \")}`).to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${selectLogicNodeName}] subset check vs [${sourceNodeName}]`);",
              "  console.log(`select_logic count = ${selectLogicIds.length}`);",
              "  console.log(`source count       = ${sourceIds.length}`);",
              "}",
              "",
              "function verifySelectLogicOrderMatchesSource(selectLogicNodeName, sourceNodeName, body) {",
              "  const selectLogicNode = verifyNodeExists(selectLogicNodeName, body);",
              "  const sourceNode = verifyNodeExists(sourceNodeName, body);",
              "  if (!selectLogicNode || !sourceNode) return;",
              "",
              "  const selectLogicOrder = pickOrderIds(selectLogicNode.result);",
              "  const sourceOrder = pickOrderIds(sourceNode.result);",
              "",
              "  pm.test(`[${selectLogicNodeName}] has ordered ids`, function () {",
              "    pm.expect(selectLogicOrder.length, \"select_logic ordered ids missing\").to.be.above(0);",
              "  });",
              "",
              "  pm.test(`[${sourceNodeName}] has ordered ids`, function () {",
              "    pm.expect(sourceOrder.length, \"Source ordered ids missing\").to.be.above(0);",
              "  });",
              "",
              "  const sourceIndexMap = {};",
              "  sourceOrder.forEach((id, idx) => {",
              "    if (!(id in sourceIndexMap)) {",
              "      sourceIndexMap[id] = idx;",
              "    }",
              "  });",
              "",
              "  pm.test(`[${selectLogicNodeName}] preserves source order from [${sourceNodeName}]`, function () {",
              "    const problems = [];",
              "",
              "    for (let i = 0; i < selectLogicOrder.length - 1; i++) {",
              "      const a = selectLogicOrder[i];",
              "      const b = selectLogicOrder[i + 1];",
              "",
              "      const aIdx = sourceIndexMap[a];",
              "      const bIdx = sourceIndexMap[b];",
              "",
              "      if (aIdx === undefined || bIdx === undefined) {",
              "        problems.push(`Missing source index: ${a}(${aIdx}), ${b}(${bIdx})`);",
              "        continue;",
              "      }",
              "",
              "      if (aIdx > bIdx) {",
              "        problems.push(",
              "          `Order mismatch at ${i}->${i + 1}: ${a} sourceIndex=${aIdx} comes after ${b} sourceIndex=${bIdx}`",
              "        );",
              "      }",
              "    }",
              "",
              "    pm.expect(problems, problems.join(\" | \")).to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${selectLogicNodeName}] order check vs [${sourceNodeName}]`);",
              "  selectLogicOrder.forEach((id, idx) => {",
              "    console.log(",
              "      `${String(idx).padStart(2, \"0\")}  ${id}  sourceIndex=${sourceIndexMap[id] ?? \"MISSING\"}`",
              "    );",
              "  });",
              "}",
              "",
              "function verifyFilterNode(nodeName, body, allowEmptyItems = true) {",
              "  const node = verifyNodeExists(nodeName, body);",
              "  if (!node) return;",
              "",
              "  const result = node.result;",
              "",
              "  pm.test(`[${nodeName}] has result`, function () {",
              "    pm.expect(result, \"node.result missing\").to.be.an(\"object\");",
              "  });",
              "",
              "  pm.test(`[${nodeName}] has result.items array`, function () {",
              "    pm.expect(Array.isArray(result && result.items), \"result.items missing\").to.equal(true);",
              "  });",
              "",
              "  const items = Array.isArray(result && result.items) ? result.items : [];",
              "",
              "  pm.test(`[${nodeName}] item_size matches items.length`, function () {",
              "    pm.expect(result.item_size, \"result.item_size missing\").to.equal(items.length);",
              "  });",
              "",
              "  if (!allowEmptyItems) {",
              "    pm.test(`[${nodeName}] items should not be empty`, function () {",
              "      pm.expect(items.length, \"result.items is empty\").to.be.above(0);",
              "    });",
              "  }",
              "",
              "  pm.test(`[${nodeName}] items are valid objects`, function () {",
              "    const invalid = items.filter(it => !it || typeof it !== \"object\");",
              "    pm.expect(invalid, \"Some items are not valid objects\").to.have.lengthOf(0);",
              "  });",
              "",
              "  console.log(`\\n[${nodeName}] items:`);",
              "  if (items.length === 0) {",
              "    console.log(\"(empty)\");",
              "    return;",
              "  }",
              "",
              "  items.forEach((it, idx) => {",
              "    const displayValue =",
              "      it && typeof it === \"object\"",
              "        ? (it.id ?? it.Id ?? it.ActivityId ?? JSON.stringify(it))",
              "        : String(it);",
              "",
              "    console.log(`${String(idx).padStart(2, \"0\")}  ${displayValue}`);",
              "  });",
              "}",
              "",
              "function verifyFilterSubsetOfPinSource(filterNodeName, sourceNodeName, body) {",
              "  const filterNode = verifyNodeExists(filterNodeName, body);",
              "  const sourceNode = verifyNodeExists(sourceNodeName, body);",
              "  if (!filterNode || !sourceNode) return;",
              "",
              "  const filterIds = pickIds(filterNode.result);",
              "  const sourceIds = unique(pickIds(sourceNode.result));",
              "",
              "  pm.test(`[${filterNodeName}] all ids must exist in [${sourceNodeName}]`, function () {",
              "    const sourceSet = new Set(sourceIds);",
              "    const missing = filterIds.filter(id => !sourceSet.has(id));",
              "    pm.expect(missing, `Filter ids not found in ${sourceNodeName}: ${missing.join(\", \")}`).to.have.lengthOf(0);",
              "  });",
              "}",
              "",
              "function verifyFilterLiveSubsetOfSource(filterNodeName, sourceNodeName, body) {",
              "  const filterNode = verifyNodeExists(filterNodeName, body);",
              "  const sourceNode = verifyNodeExists(sourceNodeName, body);",
              "  if (!filterNode || !sourceNode) return;",
              "",
              "  const filterItems = Array.isArray(filterNode.result && filterNode.result.items)",
              "    ? filterNode.result.items",
              "    : [];",
              "",
              "  const filterActivityIds = filterItems",
              "    .map(it => it && (it.ActivityId ?? it.Id ?? it.activity_id ?? it.id))",
              "    .filter(v => v !== null && v !== undefined)",
              "    .map(String);",
              "",
              "  const sourceActivityIds = unique(pickActivityIds(sourceNode.result));",
              "",
              "  pm.test(`[${filterNodeName}] live items must exist in [${sourceNodeName}] when ActivityId is present`, function () {",
              "    const sourceSet = new Set(sourceActivityIds);",
              "    const missing = filterActivityIds.filter(id => !sourceSet.has(id));",
              "    pm.expect(missing, `Live ids not found in ${sourceNodeName}: ${missing.join(\", \")}`).to.have.lengthOf(0);",
              "  });",
              "}",
              "",
              "// =============================",
              "// Entry",
              "// =============================",
              "pm.test(\"HTTP 200\", function () {",
              "  pm.response.to.have.status(200);",
              "});",
              "",
              "let body;",
              "pm.test(\"Response is valid JSON\", function () {",
              "  body = pm.response.json();",
              "  pm.expect(body).to.be.an(\"object\");",
              "});",
              "",
              "// =============================",
              "// Run",
              "// =============================",
              "",
              "// 1) select_logic must be subset of source",
              "verifySelectLogicSubsetOfSource(\"select_logic_sfv_google_trend\", \"get_google_trend\", body);",
              "verifySelectLogicSubsetOfSource(\"select_logic_sfv_relevance_creator\", \"get_redis_relevance_creator\", body);",
              "verifySelectLogicSubsetOfSource(\"select_logic_sfv_viral\", \"get_redis_viral\", body);",
              "verifySelectLogicSubsetOfSource(\"select_logic_sfv_history\", \"get_redis_history\", body);",
              "",
              "// 2) select_logic order must match source order",
              "verifySelectLogicOrderMatchesSource(\"select_logic_sfv_google_trend\", \"get_google_trend\", body);",
              "verifySelectLogicOrderMatchesSource(\"select_logic_sfv_relevance_creator\", \"get_redis_relevance_creator\", body);",
              "verifySelectLogicOrderMatchesSource(\"select_logic_sfv_viral\", \"get_redis_viral\", body);",
              "verifySelectLogicOrderMatchesSource(\"select_logic_sfv_history\", \"get_redis_history\", body);",
              "",
              "// 3) filter structure + source consistency",
              "verifyFilterNode(\"filter_pin_exclude_live\", body, false);",
              "verifyFilterNode(\"filter_live_exclude_pin\", body, true);",
              "",
              "verifyFilterSubsetOfPinSource(\"filter_pin_exclude_live\", \"get_candidate_pin_global\", body);",
              "verifyFilterLiveSubsetOfSource(\"filter_live_exclude_pin\", \"get_all_live\", body);"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "http://poc-ai-universal-platform.preprod-gcp-ai-bn.int-ai-platform.gcp.dmp.true.th/api/v1/sfv-p4?verbose=debug&ssoId=9&node_google_select_logic=by_publisher&node_pin_select_logic=by_publisher&node_live_select_logic=by_publisher&node_viral_select_logic=by_publisher&node_creator_select_logic=by_publisher&node_history_select_logic=by_publisher&shelfid=BJq5rZqYzjgJ&limit=100",
          "protocol": "http",
          "host": [
            "poc-ai-universal-platform",
            "preprod-gcp-ai-bn",
            "int-ai-platform",
            "gcp",
            "dmp",
            "true",
            "th"
          ],
          "path": [
            "api",
            "v1",
            "sfv-p4"
          ],
          "query": [
            {
              "key": "verbose",
              "value": "debug"
            },
            {
              "key": "ssoId",
              "value": "9"
            },
            {
              "key": "node_google_select_logic",
              "value": "by_publisher"
            },
            {
              "key": "node_pin_select_logic",
              "value": "by_publisher"
            },
            {
              "key": "node_live_select_logic",
              "value": "by_publisher"
            },
            {
              "key": "node_viral_select_logic",
              "value": "by_publisher"
            },
            {
              "key": "node_creator_select_logic",
              "value": "by_publisher"
            },
            {
              "key": "node_history_select_logic",
              "value": "by_publisher"
            },
            {
              "key": "cmsIds",
              "value": "zVRY3EZBmxa4",
              "disabled": true
            },
            {
              "key": "cursor",
              "value": "1",
              "disabled": true
            },
            {
              "key": "shelfid",
              "value": "BJq5rZqYzjgJ"
            },
            {
              "key": "limit",
              "value": "100"
            }
          ]
        }
      },
      "response": []
    }
  ]
}