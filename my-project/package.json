{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "upload:xray": "node upload-xray.js",
    "test:xray": "playwright test && node upload-xray.js",
    "test:xray:headed": "playwright test --headed && node upload-xray.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
